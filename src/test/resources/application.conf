# Configuration des tests pour Akka
# Ces paramètres permettent aux tests de fonctionner correctement

akka {
  log-level = WARNING
  logger-startup-timeout = 30s
}

# Configuration du dispatcher pour les tests
dispatcher {
  fork-join-executor {
    parallelism-min = 2
    parallelism-max = 8
  }
}